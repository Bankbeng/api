/**
 * Database Configuration File
 * 
 * This file stores all MySQL database connection credentials.
 * These values are used to connect to your database.
 */

module.exports = {
  HOST: "localhost",      // The server where MySQL is running (localhost = your computer)
  USER: "root",           // MySQL username (default is 'root')
  PASSWORD: "1234",       // MySQL password (the password you set during MySQL installation)
  DB: "testdb"            // Name of the database to use
};
